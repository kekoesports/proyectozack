N8N_DOMAIN=n8n.socialpro.es
GENERIC_TIMEZONE=Europe/Madrid

# Pin production images. Review release notes before changing these versions.
N8N_VERSION=2.34.6

# Generate each value independently with: openssl rand -hex 32
POSTGRES_PASSWORD=replace-with-a-random-value
N8N_ENCRYPTION_KEY=replace-with-a-random-value
RUNNERS_AUTH_TOKEN=replace-with-a-random-value

